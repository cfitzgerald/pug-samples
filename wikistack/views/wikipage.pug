extends layout

block content

  h3 #{ page.title } #[small: a(href=page.route + '/similar') Similar]
    h4 by
      |
      |
      a(href='/users/' + page.author.id) #{ page.author.name }

  div PLACEHOLDER TAG LIST
  hr

  div: a(href='/users/' + page.author.id) #{ page.author.name }

  div.page-body #{ page.content }
  hr

  a.btn.btn-primary(href=page.route + '/edit') edit this page
  a.btn.btn-danger(href=page.route + '/delete') delete this page
